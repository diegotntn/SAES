<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Boleta - IPN Campus Tlaxcala</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/boleta.css">
</head>
<body>
    <div class="header-color w-100 d-flex justify-content-between align-items-center p-3 flex-wrap">
        <img src="/images/logos-ipn.png" alt="Logo IPN" class="logo img-fluid" style="max-width: 100px;">
        <span class="text-center flex-grow-1 my-2">UNIDAD PROFESIONAL INTERDISCIPLINARIA CAMPUS TLAXCALA</span>
        <button class="btn profile-button">Perfil del Alumno</button>
    </div>
    <div class="d-flex flex-column flex-md-row flex-fill">
        <div class="sidebar p-3">
            <ul class="nav flex-column">
                <li class="nav-item"><a class="nav-link active" href="#">Datos personales</a></li>
                <li class="nav-item"><a class="nav-link" href="#">Calificaciones</a></li>
                <li class="nav-item"><a class="nav-link" href="#">Kardex</a></li>
                <li class="nav-item"><a class="nav-link" href="#">Curso actual</a></li>
                <li class="nav-item"><a class="nav-link" href="#">ETS</a></li>
                <li class="nav-item"><a class="nav-link" href="#">Solicitudes</a></li>
                <li class="nav-item"><a class="nav-link" href="#">Reinscripciones</a></li>
            </ul>
        </div>
        <div class="card flex-fill">
            <div class="card-body">
                <div class="row mb-3">
                    <div class="col-12 col-md-4"><strong>Boleta:</strong> {{student.boleta}}</div>
                    <div class="col-12 col-md-4"><strong>Nombre:</strong> {{student.nombre}}</div>
                    <div class="col-12 col-md-4"><strong>Carrera:</strong> {{student.carrera}}</div>
                    <div class="col-6 col-md-2"><strong>Plan:</strong> {{student.plan}}</div>
                    <div class="col-6 col-md-2"><strong>Promedio:</strong> {{student.promedio}}</div>
                </div>
                <table class="table table-striped">
                    <thead class="table-header">
                        <tr>
                            <th>Materia</th>
                            <th>Periodo</th>
                            <th>Forma Eval.</th>
                            <th>Calificación</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each grades}}
                        <tr>
                            <td>{{subject}}</td>
                            <td>{{period}}</td>
                            <td>{{evaluation}}</td>
                            <td>{{grade}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <footer class="mt-4 py-3 text-center">
        <div class="footer-bar">
            <p>2024 SAES</p>
            <img src="/images/logos-ipn.png" alt="Logo IPN" class="footer-logo img-fluid" style="max-width: 50px;">
            <p>IPN-DAE</p>
        </div>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
